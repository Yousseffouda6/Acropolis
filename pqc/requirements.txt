# Acropolis — Phase 7 post-quantum cryptography demo dependencies.
#
# liboqs-python exposes the `oqs` module (ML-KEM / ML-DSA via the Open Quantum
# Safe project). Install it straight from the OQS git repo — there is no reliable
# PyPI wheel, and on first install it builds the liboqs C library, so a C
# toolchain (cmake + a C compiler + libssl-dev) must be on PATH. This installs
# cleanly inside a python:3.12-slim container; see pqc/README.md.
git+https://github.com/open-quantum-safe/liboqs-python.git

# Classical RSA-3072 baseline — used only to measure a serialized public-key
# size for the post-quantum vs classical comparison table.
cryptography
