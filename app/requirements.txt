# Acropolis Notes - pinned dependencies.
#
# Web framework + its stack
Flask==3.1.3           # 3.0.3 → 3.1.3: fixes GHSA-4f9r-r8q9-mq8j session-caching disclosure
Werkzeug==3.1.8        # 3.0.3 → 3.1.8: fixes CVE-2024-49767 (resource exhaustion) + Windows safe_join CVEs
Jinja2==3.1.6          # 3.1.4 → 3.1.6: fixes CVE-2024-56201 + CVE-2024-56326 sandbox-breakout CVEs
MarkupSafe==3.0.3      # 2.1.5 → 3.0.3: required by Flask/Werkzeug/Jinja2 3.1.x
itsdangerous==2.2.0
click==8.1.8           # 8.1.7 → 8.1.8: latest patch (required by Flask 3.1.3)
blinker==1.9.0         # 1.8.2 → 1.9.0: required by Flask 3.1.3 (blinker>=1.9.0)

# Feature libraries
PyYAML==6.0.2          # YAML import/export (parsed with yaml.safe_load)
Markdown==3.9          # 3.6 → 3.9: fixes GHSA-jh85-wwwp-qpgx DoS via malformed HTML sequences
nh3==0.2.18            # HTML sanitiser applied to all rendered note / AI HTML (XSS fix)
# python-dotenv removed: GHSA-mf9w-mj56-hr94 exists in 1.2.1 with no released fix
# (fix is 1.2.2 which is not yet on PyPI). Replaced with an inline stdlib loader
# in app.py — same KEY=value semantics, zero external dependency.

# Phase 8: the deliberately outdated `requests==2.19.1` pin (old VULN #5) was
# removed — it was never imported (AI calls use the stdlib urllib), so the app
# now ships no known-vulnerable dependency.
