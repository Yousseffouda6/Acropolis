# Secrets / local env — never bake real keys into the image.
# At runtime, pass config via `docker run --env-file` or `-e`, not a baked .env.
.env
.env.*

__pycache__/
*.py[cod]
*.db
.venv/
venv/
.pytest_cache/
.DS_Store
*.log
